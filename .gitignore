# Secrets — never commit real API keys
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.streamlit/secrets.toml

# Cached API responses (src/cache.py) — regenerable, not source
cache/

# Log output (src/logging_config.py)
logs/

# Run history database (src/history.py) — generated data, not source
history.db

# OS
.DS_Store
